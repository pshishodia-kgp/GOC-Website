<hr> 
<h2><a href = "{{'/post/' + post.id|string}}">{{ post.title }}</a></h2> 
<p>{{ post.content }}</p> 

<div> 
<span class = "badge"> {{ timeago(post.published_at, datetime) }} |
    <a href = "{{ '/profile/' + post.author.username}}">{{ post.author.username }}</a> 
</span> 
<div class = "pull-right"> 
    {% with tags = post.tags.all() %}
        {% include 'tags.j2' %}
    {% endwith %}
</div> 
</div> 
<hr> 