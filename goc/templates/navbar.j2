<header class="site-header">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">
      <img src="{{ url_for('static', filename = 'logo.svg') }}" width="30" height="30" alt="" loading="lazy">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        {# <li class="nav-item active">
            <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
        </li> #}
        <li class="nav-item">
            <a class="nav-link disabled" area-disabled = "true" href="#">Problems</a>
        </li>
        
        <li class="nav-item">
            <a class="nav-link" href="/blogs">Blogs</a>
        </li>

        <li class="nav-item">
          <a class = "nav-link disabled" area-disabled = "true" href = "#">Leaderboard</a>  
        </li> 

        <li class = "nav-item"> 
          <a class = "nav-link disabled" area-disabled ="true" href = "#">Forum</a> 
        </li> 

        <li> 
          <a class = "nav-link disabled" aread-disabled = "true" href = "#">Alumnus</a> 
        </li> 

        </ul>
        <ul class = "navbar-nav"> 
          <li class = "nav-item"> 
            {% if current_user.is_authenticated %}
              <a class = "nav-link" href = "{{ url_for('logout') }}" >Logout</a>
            {% else %}
              <a class = "nav-link" href = "{{ url_for('login') }}" >Login</a>
            {% endif %} 
          </li> 

          <li class = "nav-item"> 
            {% if not current_user.is_authenticated %}
              <a class = "nav-link" href = "{{ url_for('signup') }}"> Signup</a> 
            {% endif %}
          </li> 

        </ul>
    </div>
  </nav>
</header>